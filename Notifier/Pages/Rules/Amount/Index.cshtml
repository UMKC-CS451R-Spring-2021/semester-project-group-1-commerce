@page
@model Notifier.Pages.Rules.Amount.IndexModel

@{
    ViewData["Title"] = "Index";
    Layout = "~/Pages/Shared/_Layout.cshtml";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.AmountRule[0].OwnerID)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.AmountRule[0].GreaterLess)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.AmountRule[0].amountNotification)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.AmountRule) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.OwnerID)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.GreaterLess)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.amountNotification)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.AmountRuleID">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.AmountRuleID">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.AmountRuleID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
