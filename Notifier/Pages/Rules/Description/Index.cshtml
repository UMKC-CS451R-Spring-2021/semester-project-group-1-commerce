@page
@model Notifier.Pages.Rules.Description.IndexModel

@{
    ViewData["Title"] = "Index";
    Layout = "~/Pages/Shared/_Layout.cshtml";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.DescriptionRule[0].OwnerID)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.DescriptionRule[0].descriptionNotification)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.DescriptionRule) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.OwnerID)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.descriptionNotification)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.DescriptionRuleID">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.DescriptionRuleID">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.DescriptionRuleID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
